H8: Larger fetch limit (increase from 8 to 16 positions per fetch_atlas call)

File: langslice/estimation/google/ap_multi_slice.py

--- CHANGE 1: Tool description (line 115) ---

OLD:
                "choose exactly which positions to see (1 to 8). Use this to "

NEW:
                "choose exactly which positions to see (1 to 16). Use this to "

--- CHANGE 2: positions_list slice cap (line 290) ---

OLD:
                max(pos_lo, min(pos_hi, float(p))) for p in positions_list[:8]

NEW:
                max(pos_lo, min(pos_hi, float(p))) for p in positions_list[:16]
