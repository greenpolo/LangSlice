"""Synthetic SFT trace-corpus generation and atlas region-description.

Renderer-free training-pipeline code relocated out of the former
``synthdata`` package (image synthesis moved to the standalone SimSlice
project). Import submodules directly, e.g.::

    from langslice_training.corpus.sft_corpus import generate_synthetic_rows
    from langslice_training.corpus.atlas_signature import compose_visible_clues
"""
