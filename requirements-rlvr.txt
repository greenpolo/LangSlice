# Hard pins for the langslice-gemma-4 RLVR pipeline.
# See docs/superpowers/specs/2026-05-04-gemma4-rlvr-training-design.md section 2.
#
# Verified 2026-05-04 via Context7 + Exa:
# - TRL 1.1.0 is the latest PyPI release after multimodal tool responses
#   (PR #5323/#5448) and drop-overlong tool results (PR #5521). PR #5390
#   defines stop_tool_names, but it was still open in the verified GitHub
#   source; if PyPI 1.1.0 rejects stop_tool_names, install a TRL build that
#   includes PR #5390 before running RLVR.
# - Unsloth 2026.4.8 is the latest PyPI release after the Gemma 4 RL docs and
#   installer floor bumps. Its metadata still caps trl at <=0.24.0, which is
#   older than TRL's multimodal environment_factory tool-response work.
# - Transformers 5.5.0 is the latest version allowed by Unsloth metadata and
#   the first Gemma 4 release. Later 5.5.x Gemma 4 patches exist but exceed
#   Unsloth's published <=5.5.0 cap.

trl==1.1.0
transformers==5.5.0
unsloth==2026.4.8
unsloth_zoo==2026.4.8
peft==0.19.1
torch==2.10.0
bitsandbytes==0.49.2
datasets==4.3.0
accelerate==1.13.0
trackio==0.1.0
Pillow==12.2.0
