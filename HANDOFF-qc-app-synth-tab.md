# Handoff: QC-app "Synthetic" tab after synth-module removal

**Context.** The synthetic *image*-generation code (the `augmentation` renderers,
the `microscope` optics sim, the `synthdata` package, both `synth_dataset.py`
entry points, and the bbox-detection data lane) was removed from LangSlice on
2026-06-01 and now lives in the standalone **SimSlice** project
(`C:/LabSoftware/SimSlice`). The renderer-free training pieces
(`sft_corpus.py`, `atlas_signature.py`) were relocated to
`models/training-core/langslice_training/corpus/`.

**Why this is a handoff and not a direct edit.** The QC app
(`models/data/langslice_data/qc_app/`) is in the protected synth-lane that the
removal session was told not to touch. The changes below are correct but need a
qc-app-lane session (or your explicit go-ahead) to apply.

## Current state (no crash)

- The QC app still **runs fine**. The Synthetic route `_handle_synth_datasets`
  (`qc_app/app.py:1757, 2354`) only *reads* the `_local/synth_data/` output dir
  and returns empty gracefully when it is missing. It imports **none** of the
  removed modules.
- The tab is now **dormant**: any pre-existing `_local/synth_data/` content still
  displays, but LangSlice can no longer (re)generate it — that capability moved
  to SimSlice.

## Stale references to fix

1. `qc_app/static/app.html:2345` — empty-state hint still says:
   `no synth data yet — run python -m synth_dataset write --out _local/synth_data --n 100`.
   That command no longer exists. Suggested replacement text:
   `no synth data — synthetic image generation now lives in the SimSlice project (C:/LabSoftware/SimSlice).`

2. `qc_app/CONTRACTS.md:261-289` (the "Synthetic" contract section) — update:
   - line 263: `Default output of python -m synth_dataset write ...` → command removed.
   - line 266: `Per-line JSON, generated by models/langslice-gemma-4/data/synth_dataset.py` →
     that file is deleted.
   Recommended: re-document the Synthetic layer as **read-only / legacy** (it
   displays pre-existing `_local/synth_data/` only), or note that population is
   now done in SimSlice and the manifest format is unchanged.

## Optional product decision (yours)

Decide whether to **remove the Synthetic tab entirely** from the QC app
(`_handle_synth_datasets` route + the front-end tab) since LangSlice can no
longer populate it, or **keep it as a read-only viewer** for any existing
`_local/synth_data/` content. No code change is required for it to keep working;
this is purely about whether the tab still earns its place.

---
*Generated during the SimSlice extraction. Safe to delete after dispatching.*
